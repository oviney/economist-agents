# Agent state — ignore runtime data but commit slash commands and agent personas
.claude/*
!.claude/commands/
!.claude/agents/
.gemini/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
.venv
.venv-*/

# Agent outputs
content_queue.json
board_decision.json
board_report.md
test_outputs/
model_comparison_results.json
output/

# Runtime telemetry / eval logs (appended-to by tests + pipeline)
logs/article_evals.json
logs/agent_sdk_costs.jsonl

# Secrets
.env
*.key
*_API_KEY*
*.json.bak
*-sa.json

# Google credentials
.google/

# Local data (SQLite, performance metrics)
data/*
# Track orchestrator audit-trail (starts as empty [])
!data/orchestrator_state.json
# Track the article review corpus — labelled evaluation fixtures for the
# citation-integrity and claim-provenance gates (B-020/B-021). These are the
# regression inputs, not runtime state, so they belong in version control.
!data/review_corpus/
!data/review_corpus/**

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Local databases (cached by GitHub Actions)
.chromadb/

# Runtime logs (written by ROI tracker and tests — should not be tracked)
logs/execution_roi.json
logs/quality_dashboard.json
.coverage

# Playwright MCP artifacts
.playwright-mcp/

# MkDocs build output
site/

# Screenshot artifacts
ga4-*.png
search-console-*.png
analytics-tab.png


# Temporary docs review screenshots
docs-site-*.png
github-pages-*.png

home.png
home-*.png
