# Core dependencies
# CrewAI framework (Phase 1 migration - ADR-003)
# Development dependencies
# Economist Agents - Python Dependencies
# Optional dependencies
# Security (recommended)
crewai-tools>=0.12.0     # CrewAI tools for agent capabilities
chromadb>=0.4.0          # Vector database for Style Memory RAG (STORY-006)
matplotlib>=3.7.0        # Chart generation for graphics agent
numpy>=1.24.0            # Data manipulation for charts
openai>=1.0.0            # OpenAI API (required)
PyGithub>=2.1.0          # GitHub API integration for sprint sync
pytest>=7.0.0            # Testing framework (optional)
python-dotenv>=1.0.0     # Secure .env file loading
python-slugify>=8.0.0    # URL-safe filenames
pyyaml>=6.0              # YAML parsing for config
orjson>=3.9.0
redis>=4.0.0             # Optional Redis cache backend (Story #145)
requests>=2.31.0         # HTTP requests for image generation
