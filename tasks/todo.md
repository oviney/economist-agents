# TODO: Fix shell injection in regenerate-image.yml (#342)

## In Progress

## Pending

- [ ] T1 — Add job-level `env:` block + `Validate inputs` step
- [ ] T2 — Replace all 16 `${{ inputs.* }}` references in `run:` blocks
- [ ] T3 — Grep verification + close #342

## Done
