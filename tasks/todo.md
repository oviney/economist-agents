# TODO: Repo Process Consolidation

**Plan**: [`tasks/plan.md`](./plan.md) · **Spec**: [`docs/specs/repo-process-consolidation.md`](../docs/specs/repo-process-consolidation.md)

## Wave 1 — Safe retirement ✅ (pushed to `claude/repo-process-agent-skills-p0h0op`)

- [x] **W1-0**: Spec correction + plan/todo (coupling audit) — commit `plan:`
- [x] **W1-A**: Deleted stale snapshots (SPRINT_15 report set, SPRINT_10 cert, 3 badge JSONs) + trimmed README badges — commit `chore: retire stale sprint/badge…`
- [x] **W1-B**: Deleted `skills/sprint-management/` + `skills/scrum-master/` + mkdocs nav — commit `chore: retire sprint-management…` (validate_skills 37/37 ✓)
- [x] **W1-C**: Deleted `sprint-discipline.yml` + `sprint-sync.yml` + dropped sprint_validator step from `quality-tests.yml` — commit `chore: retire sprint-discipline…` (YAML ✓)
- [x] **W1-D**: Collapsed `GEMINI.md` → pointer — commit `docs: collapse GEMINI.md…`
- [ ] **Checkpoint**: confirm `ci.yml` / `quality-tests.yml` / `docs.yml` green in CI

### Deferred out of Wave 1 during the coupling audit (were "obvious" but entangled)
- `SPRINT.md` — read by KEEP `src/backlog/validate_documentation_accuracy.py`, `scripts/defect_prevention_rules.py`, and 2 tests → delete in W2 with them.
- `.deployment_state` — **KEEP (live)**, used by `scripts/rollback_production.sh`.
- root `SPEC.md` — referenced by `src/quality/visual_qa_zones.py` → W2.
- `.github/BACKLOG.md` — referenced by `src/backlog/migrate_backlog_to_github.py` → W2.
- `run_full_workflow.sh`, `test_governance.sh` — referenced by `docs/guides/WORKFLOW_GUIDE.md` → W2 (with doc fix).
- Root + `.github/copilot-instructions.md` + CONTRIBUTING dedup — `.github` copy is auto-generated by `sync-copilot.yml`/`sync_copilot_context.py`; consolidate with W2-C so it isn't regenerated.

## Wave 2 — Deferred (needs human decision / ADR + runnable test suite — NOT in this pass)

- [ ] **W2-A**: Decide keep-vs-retire the AgentRegistry system (ADR-002): 10 `.github/agents/*.agent.md` personas + `AGENTS.md` + `scripts/agent_registry.py` + `test_llm_providers.py`/`test_agent_registry_enhancement.py`/`test_architect_agent.py`. **← blocking decision**
- [ ] **W2-B**: Retire `sm/po/orchestrator_agent`, `continuous_burndown`, `sprint_validator` + their tests; delete `SPRINT.md`; disentangle `sprint_tracker/task_queue/escalations` writers in `src/backlog/*`.
- [ ] **W2-C**: Retire `nightly-eval.yml` + `scripts/benchmarks/measure_sm_effectiveness.py` + `sync-copilot.yml`; then consolidate the copilot-instructions files.
- [ ] **W2-D**: Delete root `SPEC.md`, `.github/BACKLOG.md`, sprint ISSUE_TEMPLATEs + fix their KEEP references.

**Wave 2 requires a working `pytest` (the env here can't build the feedparser/`sgmllib3k` wheel), so it must run where the full suite is green.**
